/**
 * Dynamic Programming
 * Interview Bit - Pick from both sides.
 */


const input = [5, -2, 3, 1, 2];

function main(A, B) {
    const input_array = A;
    const total_nums = B;
    let greatestSum = 0;
    let i = 0;
    const mid = Math.floor(input_array.length / 2);
    while (i < total_nums) {

        i++;
    }
}
